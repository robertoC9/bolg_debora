document.addEventListener('DOMContentLoaded', () => {
  // Este archivo existe para que cada subcarpeta (sección secundaria)
  // tenga su propio JS.
  // Por ahora, la lógica específica está en historias.js.
});

